using Gtk 4.0;
using Adw 1;

// Bad Example
Adw.Clamp clamp {
  ListView {}
}

// Good Example
Adw.ClampScrollable clamp_scrollable {
  ListView {}
}

// Extern type should not trigger the lint
Adw.Clamp clamp2 {
  $MyScrollable {}
}
